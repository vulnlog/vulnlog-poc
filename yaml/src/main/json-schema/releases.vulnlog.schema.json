{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vulnlog.dev/releases.vulnlog.schema.json",
  "title": "Vulnlog Releases Schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "title": "Release Identifier",
        "description": "Identifier of the release. Must be unique.",
        "type": "string"
      },
      "name": {
        "title": "Release Name",
        "description": "Printable name of the release.",
        "examples": [ "Product 1.0.0" ],
        "type": "string"
      },
      "release-date": {
        "title": "Release Date",
        "description": "Date of the release. Format: YYYY-MM-DD.",
        "type": "string",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
        "format": "date"
      },
      "version": {
        "title": "Release Version",
        "description": "Version of the release. For example in the semantic version format: X.Y.Z.",
        "examples": [ "1.0.0" ],
        "type": "string"
      }
    }
  }
}
