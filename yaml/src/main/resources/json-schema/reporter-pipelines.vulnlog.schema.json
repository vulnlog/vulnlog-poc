{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vulnlog.dev/reporter-pipelines.vulnlog.schema.json",
  "title": "Vulnlog Reporter Pipeline Schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "title": "Reporter Pipeline Identifier",
        "description": "Identifier of this reporter pipeline. Must be unique.",
        "type": "string"
      },
      "lifetime": {
        "title": "Pipeline Lifetime",
        "description": "From when until when this pipeline is active.",
        "type": "object",
        "properties": {
          "end": {
            "title": "End date of this pipeline",
            "description": "Date when this pipeline is no longer active. Format: YYYY-MM-DD",
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "format": "date"
          },
          "start": {
            "title": "Start date of this pipeline",
            "description": "Date from when this pipeline is active. Format: YYYY-MM-DD",
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "format": "date"
          }
        }
      },
      "name": {
        "title": "Reporter Pipeline Name",
        "description": "Name of this reporter pipeline.",
        "type": "string"
      },
      "reporter-ref-id": {
        "title": "Reporter",
        "description": "Reporter which is used by this pipeline. The reporter's unique identifier is required.",
        "type": "string"
      }
    }
  }
}
