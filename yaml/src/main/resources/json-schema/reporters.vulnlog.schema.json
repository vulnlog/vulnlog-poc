{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vulnlog.dev/reporters.vulnlog.schema.json",
  "title": "Vulnlog Reporters Schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "title": "Reporter Identifier",
        "description": "Identifier of the reporter. Must be unique. Example: \"trivy\".",
        "type": "string"
      },
      "name": {
        "title": "Reporter Name",
        "description": "Printable name of the reporter. Example: \"OWASP Dependency Check\".",
        "type": "string"
      },
      "vendor": {
        "title": "Reporter Vendor",
        "description": "An identifier describing the reporter vendor. This identifier should be used later to match predefined templates for suppression files for example.",
        "enum": [
          "Aqua Security Trivy",
          "OWASP Dependency Check",
          "Snyk Open Source"
        ]
      }
    }
  }
}
